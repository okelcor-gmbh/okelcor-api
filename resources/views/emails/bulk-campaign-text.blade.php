{{-- Plain-text alternative, rendered from the campaign's blocks by
     CampaignBlockRenderer::renderText(). Merge tags (including
     [[UNSUBSCRIBE_URL]]) are already substituted per recipient before this
     view runs, same as the HTML part. --}}
{!! $bodyText !!}
